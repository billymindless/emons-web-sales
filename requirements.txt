# 배포 환경(Streamlit Cloud)에서 매 배포마다 uv가 "최신" 버전으로 재설치하면서
# 로컬에서 검증되지 않은 numpy/pyarrow 등 네이티브 확장 조합이 Segfault를 유발한 바 있음.
# → 로컬에서 장시간 안정 구동이 검증된 버전으로 전체 고정 (재현성 확보).
streamlit==1.54.0
streamlit-autorefresh==1.0.1
streamlit-quill==0.0.3
pandas==2.3.3
numpy==2.4.2
pyarrow==23.0.0
requests==2.32.5
openpyxl==3.1.5
plotly==6.5.2
matplotlib==3.10.5
reportlab==4.4.3
folium==0.20.0
streamlit-folium==0.26.1
supabase==2.28.0
psycopg2-binary==2.9.12
# 채널톡 웹훅 API 서버 (api.py)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx==0.28.1
# Gmail 연동 (gmail_manager.py)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.127.0
# AI VOC 분석 (Gemini REST API — httpx로 직접 호출, 별도 패키지 불필요)
# 엘리베이터 사이즈 점검 (공공데이터포털 XML 응답 파싱; 3D 시각화는 기존 plotly 재사용)
xmltodict==1.0.4
holidays==0.97
