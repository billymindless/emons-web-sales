-- =====================================================================
-- app_orders 에 import_source (임포트 출처) 컬럼 추가
-- =====================================================================
-- 목적: 과거 엑셀 데이터 일괄 임포트 시 이력 식별·롤백을 위한 태그 컬럼.
--   - NULL       : 앱 정상 등록 (기존 데이터)
--   - '엑셀_과거이력' : "과거 구매내역 일괄 임포트" UI 로 삽입된 행
-- 조회 성능이 아닌 감사·롤백 용도이므로 인덱스는 만들지 않는다.
-- =====================================================================

ALTER TABLE app_orders
    ADD COLUMN IF NOT EXISTS import_source TEXT;
